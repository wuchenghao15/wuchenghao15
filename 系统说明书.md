# MTSCOS AI Project 系统说明书

## 1. 系统概述

MTSCOS AI Project 是一个基于 Flask 框架的智能系统，集成了多种 AI 功能和用户管理系统。系统采用模块化设计，支持智能用户管理、考试系统、AI 引擎等功能。

### 1.1 系统架构

- **前端**：基于 HTML5、CSS3 和 JavaScript 的响应式 web 界面
- **后端**：Flask 框架，Python 3.8+
- **数据库**：SQLite（本地）/ MySQL（可选）
- **AI 引擎**：集成多种 AI 模型和智能分析功能
- **安全**：密码加密、令牌验证、IP 黑名单等安全机制

## 2. 目录结构

```
MTSCOS_AI_Project/
├── flask-app/            # 主应用目录
│   ├── app/              # 应用核心代码
│   │   ├── ai/           # AI 相关模块
│   │   ├── blueprints/   # 蓝图路由
│   │   ├── config/       # 配置文件
│   │   ├── models/       # 数据模型
│   │   ├── services/     # 服务层
│   │   ├── utils/        # 工具类
│   │   ├── views/        # 视图函数
│   │   └── routes/       # 路由管理
│   ├── templates/        # 前端模板
│   ├── static/           # 静态资源
│   ├── app.py            # 应用入口
│   └── requirements.txt  # 依赖包
├── system_size.log.*     # 系统大小日志
├── system_time.log.*     # 系统时间日志
└── 系统说明书.md         # 本说明书
```

## 3. 核心功能模块

### 3.1 用户认证系统

- **注册功能**：支持新用户注册，包含密码强度验证
- **登录功能**：支持用户名/密码登录
- **忘记密码**：基于邮箱的密码重置功能
- **用户管理**：支持用户信息查看和管理

### 3.2 考试系统

- **题库管理**：支持多种题型，包括听力题
- **考试管理**：创建和管理考试
- **成绩管理**：记录和查询考试成绩

### 3.3 AI 引擎

- **智能分析**：用户行为分析和异常检测
- **学习系统**：AI 自学习和优化
- **监控系统**：系统状态监控和报告

### 3.4 系统管理

- **安全管理**：IP 黑名单、用户黑名单
- **权限管理**：基于角色的权限控制
- **日志管理**：系统日志记录和分析

## 4. 数据库设计

### 4.1 用户表（user）

| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | INTEGER | 用户ID |
| username | TEXT | 用户名 |
| email | TEXT | 邮箱 |
| password | TEXT | 密码（加密） |
| role | TEXT | 角色 |
| is_active | INTEGER | 是否激活 |
| super_admin_approved | INTEGER | 超级管理员审批 |
| hardware_admin_approved | INTEGER | 硬件管理员审批 |
| avatar | TEXT | 头像 |
| reset_token | TEXT | 重置密码令牌 |
| reset_token_expiry | TEXT | 令牌过期时间 |
| password_modified_at | TIMESTAMP | 密码修改时间 |
| password_modified_by | TEXT | 密码修改人 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 4.2 用户组表（user_groups）

| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | INTEGER | 记录ID |
| user_id | INTEGER | 用户ID（唯一） |
| group_name | TEXT | 组名 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 4.3 密码历史表（password_history）

| 字段名 | 类型 | 描述 |
|-------|------|------|
| id | INTEGER | 记录ID |
| user_id | INTEGER | 用户ID |
| password_hash | TEXT | 密码哈希 |
| created_at | TIMESTAMP | 创建时间 |

### 4.4 其他表

- **user_login_history**：用户登录历史
- **login_attempts**：登录尝试记录
- **ip_blacklist**：IP 黑名单
- **user_blacklist**：用户黑名单

## 5. 系统配置

### 5.1 环境变量

系统支持通过环境变量配置数据库连接、AI 引擎设置等参数。

### 5.2 配置文件

主要配置文件位于 `app/config/` 目录，包括：
- `config.py`：主配置文件
- `services_config.json`：服务配置

## 6. 系统启动

### 6.1 依赖安装

```bash
# 安装依赖
pip install -r flask-app/requirements.txt

# 启动应用
cd flask-app
python app.py
```

### 6.2 访问地址

默认访问地址：`http://localhost:8000`

## 7. 安全机制

### 7.1 密码安全
- 密码使用安全哈希算法加密存储
- 密码强度验证（长度、复杂度）
- 密码历史记录，防止重复使用旧密码
- 常用密码检测

### 7.2 访问控制
- 基于角色的权限控制
- IP 黑名单机制
- 登录尝试限制

### 7.3 数据保护
- 敏感数据加密存储
- 令牌过期机制
- 安全的密码重置流程

## 8. 用户组管理

系统采用排他性用户组设计，每个用户只能属于一个组：

- **admin**：管理员组
- **super_admin**：超级管理员组
- **hardware_admin**：硬件管理员组
- **user**：普通用户组
- **developer**：开发人员组
- **tester**：测试人员组

## 9. 系统维护

### 9.1 日志管理
- 系统日志：`app/logs/` 目录
- 系统大小日志：`system_size.log.*`
- 系统时间日志：`system_time.log.*`

### 9.2 数据库维护
- 定期备份数据库
- 优化数据库性能
- 清理过期数据

### 9.3 安全更新
- 定期更新依赖包
- 检查安全漏洞
- 更新安全策略

## 10. 故障排除

### 10.1 常见问题

| 问题 | 可能原因 | 解决方案 |
|------|---------|----------|
| 数据库连接失败 | 配置错误 | 检查数据库配置 |
| 密码重置失败 | 令牌过期 | 重新请求重置链接 |
| AI 加载失败 | 依赖缺失 | 安装必要的依赖包 |
| 端口被占用 | 其他服务占用 | 停止占用端口的服务 |

### 10.2 错误日志

系统错误会记录到 `app/logs/system.log` 文件，可通过查看日志定位问题。

## 11. 未来规划

- 增强 AI 功能，支持更多智能分析
- 优化系统性能，支持更大规模用户
- 增加更多考试题型和功能
- 提升系统安全性和可靠性
- 支持更多数据库类型和云部署

## 12. 联系信息

- 系统维护：admin@mtscos.com
- 技术支持：support@mtscos.com
- 系统版本：1.0.0
- 最后更新：2026-04-06
