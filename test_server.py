#!/usr/bin/env python3
"""
测试服务器，用于检查端口是否可用

from flask import Flask

app = Flask(__name__)

@app.route('/')
def hello():
    return "Hello, World! This is a test server running on port 8888"

if __name__ == '__main__':
    print("启动测试服务器，监听端口 8888...")
    app.run(host='0.0.0.0', port=8888, debug=True)

"""