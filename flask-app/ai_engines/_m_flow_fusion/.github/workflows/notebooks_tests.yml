# M-Flow — notebook suite entry (orchestrator calls reusable_notebook).
# -----------------------------------------------------------------------------
# Active job: multimedia demo path; commented block kept for local toggles.
# -----------------------------------------------------------------------------
name: "notebook-tests"

concurrency:
  cancel-in-progress: false
  group: mflow-notebook-suite-${{ github.run_id }}

on:
  workflow_call:

permissions: {contents: read}

jobs:
  # run-main-notebook:
  #   name: Core demo notebook
  #   uses: ./.github/workflows/reusable_notebook.yml
  #   with:
  #     notebook-location: notebooks/m_flow_demo.ipynb
  #   secrets: inherit

  mflow_multimedia_nb_orchestrator:
    name: "Multimedia demo notebook"
    uses: ./.github/workflows/reusable_notebook.yml
    with:
      notebook-location: notebooks/m_flow_multimedia_demo.ipynb
    secrets: inherit
