# m_flow.shared.infra_data
# Text segmentation engines, keyword utilities, and exception types
# that support the data-processing layer of the pipeline.
#
# Sub-packages:
#   chunking — pluggable backends (default, langchain, haystack)
#   exceptions — domain-specific error classes
#   utils — small shared helpers

_PKG_LABEL = "infra-data"
