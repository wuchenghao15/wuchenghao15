"""
M-flow CLI command implementations.

This package contains individual command handlers for the
mflow command-line interface, including:

- add: Add data to M-flow
- memorize: Process and index data
- search: Query the knowledge base
- update: Update existing data
- delete: Remove data
"""

from __future__ import annotations

# Command modules loaded on demand by the CLI router
