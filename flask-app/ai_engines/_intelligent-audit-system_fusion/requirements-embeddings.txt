# Optional local semantic-embedding runtime.
# Install only when RAG_ENABLE_EMBEDDINGS=1 is required. Keeping this separate
# avoids shipping Torch/Transformers in the default web service image.
-r requirements.txt
sentence-transformers>=5.6.1,<6.0
transformers>=5.14.1,<6.0
torch>=2.13.0,<3.0
pillow>=12.3.0,<13.0
filelock>=3.32.0,<4.0
